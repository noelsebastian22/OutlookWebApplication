import { FilterFlagPipe } from './filter-flag.pipe';

describe('FilterFlagPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterFlagPipe();
    expect(pipe).toBeTruthy();
  });
});
